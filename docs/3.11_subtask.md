Phase 1 — SubTask 功能
- src/store/kanban/index.ts：新增 SubTask 類型，Task 類型加了 subTasks: SubTask[]，新增 addSubTask / removeSubTask / toggleSubTask / renameSubTask / linkSubTask 五個 actions，persist version 升到 2 並做好舊數據遷移
- src/components/items/LinkSubTaskDialog.tsx（新建）：兩步選擇器，先選 Note 再選該 Note 的 Block
- src/components/Board/Task.tsx：Task 標題欄顯示 完成數/總數，點擊 › / ⌄ 展開/折疊；展開後每行 SubTask 有 checkbox（切換完成）、雙擊行內編輯 title、link 按鈕（藍色表示已連結，點擊 SubTask 標題跳轉到對應 Note）、刪除按鈕；最右側有 + 按鈕快速添加 SubTask
Phase 2 — 拖拽排序
安裝 @dnd-kit/sortable + @dnd-kit/utilities
- src/store/kanban/index.ts：新增 missionOrder / boardOrder 有序 ID 數組，reorderMissions / reorderBoards / reorderTasks actions；moveTask 支持 targetIndex 位置插入
- src/components/Mission/index.tsx：改為 useSortable，帶拖拽手柄
- src/components/Board/BoardItem.tsx：改為 useSortable，帶拖拽手柄
- src/components/Board/index.tsx：Board 列表用 SortableContext（水平），每個 Board 內 Task 列表用 SortableContext（垂直）
- src/pages/workPage/index.tsx：DndContext 加 PointerSensor（5px 啟動閾值防誤觸），SortableContext 包裹 Mission 列表，DragEnd 根據 type（mission / board / task）分別調用 reorderMissions / reorderBoards / reorderTasks / moveTask，支持同 Board 內排序和跨 Board 移動並插入到指定位置